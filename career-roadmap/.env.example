# Anthropic — used server-side only, in app/api/generate-roadmap/route.ts
ANTHROPIC_API_KEY=sk-ant-...

# Supabase — used server-side only (service role key), for the analytics
# `runs` table. See supabase/schema.sql. Never prefix these with NEXT_PUBLIC_.
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
